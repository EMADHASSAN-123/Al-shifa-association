# موقع مركز الشفاء لتعليم القرآن الكريم

موقع عربي احترافي ومتكامل لمركز تعليم القرآن الكريم، مبني باستخدام HTML و Tailwind CSS و JavaScript، مع تكامل كامل مع Supabase لإدارة المحتوى الديناميكي.

## المميزات

### 🌐 الصفحات الرئيسية
- **الصفحة الرئيسية**: عرض شامل للمركز مع أقسام الدورات، الإنجازات، التلاوات، المعرض، والأخبار
- **صفحة عن المركز**: معلومات شاملة عن الخلفية، الأهداف، الفلسفة التعليمية، والإدارة
- **صفحة المواد التعليمية**: خطط دراسية ومواد قابلة للتنزيل
- **صفحة الخدمات والبرامج**: قائمة البرامج وآلية التسجيل
- **صفحة الإنجازات**: المشاريع المنجزة، الشهادات، وآراء الطلاب
- **صفحة المعرض**: معرض للصور، الفيديوهات، والتلاوات الصوتية
- **صفحة التواصل**: نموذج اتصال وخرائط

### 🎨 التصميم
- تصميم حديث ومتناسق بألوان بني وبيج
- دعم كامل للغة العربية و RTL
- متجاوب مع جميع الأجهزة (موبايل، تابلت، ديسكتوب)
- استخدام خط Cairo العربي الواضح
- أيقونات Font Awesome

### 🛠️ لوحة التحكم الإدارية (متكاملة مع Supabase)
- **لوحة التحكم الرئيسية**: إحصائيات ديناميكية من قاعدة البيانات
- **إدارة المحتوى**: إضافة، تعديل، وحذف المحتوى مع Supabase
- **إدارة الوسائط**: رفع وإدارة الملفات مع Supabase Storage
- **أدوات الذكاء الاصطناعي**: إنشاء محتوى تلقائياً
- **نظام تسجيل الدخول**: حماية لوحة التحكم مع Supabase Auth

### 🤖 أدوات الذكاء الاصطناعي
- توليد وصف للدورات
- صياغة منشورات إخبارية
- إعادة صياغة النصوص
- كتابة خطط تعليمية

## هيكل المشروع

```
cursor2/
├── index.html              # الصفحة الرئيسية
├── about.html              # صفحة عن المركز
├── courses.html            # صفحة المواد التعليمية
├── services.html           # صفحة الخدمات والبرامج
├── achievements.html        # صفحة الإنجازات
├── gallery.html            # صفحة المعرض
├── contact.html            # صفحة التواصل
├── js/
│   ├── main.js            # JavaScript الرئيسي
│   ├── gallery.js         # وظائف المعرض
│   ├── contact.js         # وظائف نموذج التواصل
│   ├── admin.js           # وظائف لوحة التحكم
│   └── ai-tools.js        # وظائف أدوات الذكاء الاصطناعي
├── admin/
│   ├── dashboard.html     # لوحة التحكم الرئيسية
│   ├── content-management.html  # إدارة المحتوى
│   ├── ai-tools.html      # أدوات الذكاء الاصطناعي
│   └── media.html         # إدارة الوسائط
└── README.md              # هذا الملف
```

## المتطلبات

- حساب Supabase (مجاني)
- متصفح حديث
- اتصال بالإنترنت

## الإعداد الأولي

### 1. إعداد Supabase

اتبع التعليمات الكاملة في ملف [`SUPABASE_SETUP.md`](SUPABASE_SETUP.md)

**ملخص سريع:**
1. أنشئ مشروع Supabase جديد
2. أنشئ جدول `content` مع الأعمدة المطلوبة
3. أنشئ Storage bucket باسم `media`
4. حدّث المفاتيح في ملفات JavaScript:
   - `js/supabase-config.js`
   - `js/auth.js`
   - `js/supabase-content.js`

### 2. تحديث المفاتيح

في جميع ملفات JavaScript المذكورة أعلاه، استبدل:
- `YOUR_SUPABASE_URL` بـ Project URL من Supabase
- `YOUR_SUPABASE_ANON_KEY` بـ anon key من Supabase

## طريقة التشغيل

### الطريقة الأولى: فتح الملفات مباشرة
1. قم بتحميل جميع الملفات
2. حدّث مفاتيح Supabase
3. افتح ملف `index.html` في المتصفح
4. الموقع سيعمل مباشرة بدون حاجة لخادم

### الطريقة الثانية: استخدام خادم محلي
إذا كنت تريد تجربة أفضل (خاصة للـ API calls):

#### باستخدام Python:
```bash
# Python 3
python -m http.server 8000

# ثم افتح المتصفح على:
http://localhost:8000
```

#### باستخدام Node.js:
```bash
# تثبيت http-server
npm install -g http-server

# تشغيل الخادم
http-server -p 8000

# ثم افتح المتصفح على:
http://localhost:8000
```

#### باستخدام PHP:
```bash
php -S localhost:8000
```

## الميزات الديناميكية

### إدارة المحتوى
- جميع المحتويات (أخبار، دورات، ملفات) مخزنة في Supabase
- يمكن إضافة/تعديل/حذف المحتوى من لوحة التحكم
- المحتوى المنشور يظهر تلقائياً في الموقع العام

### إدارة الوسائط
- رفع الصور والفيديوهات والملفات إلى Supabase Storage
- إدارة كاملة للملفات من لوحة التحكم
- روابط عامة للملفات

### نظام المصادقة
- تسجيل دخول آمن للمديرين
- حماية جميع صفحات لوحة التحكم
- إدارة الجلسات مع Supabase Auth

## التخصيص

### تغيير الألوان
الألوان الحالية (بني وبيج) محددة في كل صفحة باستخدام Tailwind CSS. يمكنك تغييرها من:
- `bg-amber-*` للألوان البنية
- `text-amber-*` لنصوص الألوان

### إضافة محتوى
1. سجل الدخول إلى لوحة التحكم (`admin/login.html`)
2. اذهب إلى "إدارة المحتوى"
3. اضغط "إضافة محتوى جديد"
4. املأ النموذج واختر "نشر المحتوى"
5. المحتوى سيظهر تلقائياً في الموقع
 
### ربط API الذكاء الاصطناعي
1. افتح ملف `js/ai-tools.js`
2. ابحث عن دالة `generateContent()`
3. استبدل الكود التجريبي باستدعاء API حقيقي:

```javascript 
async function generateContent() {
    const input = document.getElementById('aiInput').value;
    const response = await fetch('YOUR_API_ENDPOINT', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': 'Bearer YOUR_API_KEY'
        },
        body: JSON.stringify({
            prompt: input,
            // other parameters
        })
    });
    const data = await response.json();
    // Display the generated content
}
```

## المتطلبات

- متصفح حديث (Chrome, Firefox, Safari, Edge)
- اتصال بالإنترنت (لتحميل Tailwind CSS و Font Awesome من CDN)

## هيكل قاعدة البيانات

### جدول `content`
- `id`: معرف فريد
- `type`: نوع المحتوى (courses, news, files, pages)
- `title`: العنوان
- `description`: الوصف
- `image_url`: رابط الصورة
- `file_url`: رابط الملف (للملفات)
- `file_name`: اسم الملف
- `published`: حالة النشر (true/false)
- `created_at`: تاريخ الإنشاء
- `updated_at`: تاريخ التحديث

### Supabase Storage
- **Bucket**: `media`
- **المجلدات**: `images/`, `videos/`, `audio/`, `documents/`

## الأمان

- جميع صفحات لوحة التحكم محمية بنظام تسجيل الدخول
- Row Level Security (RLS) مفعّل على جميع الجداول
- الوصول العام للمحتوى المنشور فقط
- الوصول للمحتوى غير المنشور للمديرين فقط

## الميزات المستقبلية المقترحة

- [x] ربط قاعدة بيانات حقيقية (Supabase)
- [x] نظام تسجيل دخول للمديرين
- [ ] نظام حجز الدورات
- [ ] دفع إلكتروني
- [ ] تطبيق موبايل
- [ ] نظام إشعارات
- [ ] تحليلات الموقع

## الدعم

لأي استفسارات أو مشاكل، يرجى التواصل مع فريق التطوير.

## الترخيص

هذا المشروع مخصص لمركز الشفاء لتعليم القرآن الكريم.

---

**تم التطوير بواسطة:** فريق التطوير  
**التاريخ:** 2024  
**الإصدار:** 1.0.0

